name: 📊 GitHub Metrics

on:
  schedule:
    - cron: "0 17 * * *"   # 00:00 WIB (UTC+7)
  workflow_dispatch:

jobs:
  metrics:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write

    steps:
      - name: 🔍 Check GitHub activity in last 23 hours
        id: activity
        run: |
          USER="ClasherGeek77"
          SINCE=$(date -u -d "23 hours ago" +"%Y-%m-%dT%H:%M:%SZ")

          COUNT=$(curl -s https://api.github.com/users/$USER/events \
            | jq --arg SINCE "$SINCE" '[.[] | select(.created_at > $SINCE)] | length')

          echo "Events in last 23h: $COUNT"

          if [ "$COUNT" -eq 0 ]; then
            echo "No activity detected — skipping metrics update."
            echo "skip=true" >> $GITHUB_OUTPUT
          fi

      - name: 📊 GitHub Stats Card
        if: steps.activity.outputs.skip != 'true'
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: metrics/stats.svg
          base: header, repositories
          plugin_lines: yes
          config_display: columns
          output_action: commit

      - name: 🗂️ Top Languages
        if: steps.activity.outputs.skip != 'true'
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: metrics/languages.svg
          base: ""
          plugin_languages: yes
          plugin_languages_indepth: yes
          plugin_languages_details: percentage
          plugin_languages_limit: 8
          output_action: commit
